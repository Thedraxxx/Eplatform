import { Response } from "express";
import asyncHandler from "../../../utils/asyncHandler";
import { IExtedREquest } from "../../../utils/types";


const createCategory = asyncHandler(async(req: IExtedREquest, res: Response)=>{
         
})

